# NutriAgent 固定 Demo 场景

## 目标

用一条稳定、可重复的演示路线，把这个项目最有价值的三件事讲清楚：

- 这是一个真实健康管理闭环，不是普通聊天 Demo
- 这是一个会拆任务、会调工具、会追问的 Agent 项目
- 这是一个移动端优先的 AI 产品，而不是一堆接口拼出来的页面

## 推荐演示总时长

- 3 分钟版：适合面试现场快速讲解
- 6 分钟版：适合录屏和作品展示

## 固定 Demo 路线

### Step 1. 展示建档入口

讲法：

“我先让用户选择不同人格的 AI 私教，然后用移动端的一题一屏方式完成建档。这里采集的不只是基础资料，也包括目标、运动习惯和健康约束，后面会直接进入 Agent 上下文。”

操作：

- 打开 `/onboarding`
- 展示教练选择
- 快速说明目标、体重、运动习惯、健康史这些字段为什么重要

### Step 2. 展示今日健康工作台

讲法：

“首页不是普通 dashboard，而是今天的执行工作台。用户会先看到剩余热量、摄入、消耗、热量缺口和按餐次组织的数据。”

操作：

- 打开首页 `/`
- 展示日期切换、本周目标、剩余热量、营养进度
- 强调底部主操作区可以直接去记餐、拍照或进入 Agent

### Step 3. 记录一顿晚餐

固定输入：

`晚餐吃了 180g 鸡胸肉、100g 米饭和一份生菜`

讲法：

“这里支持自然语言解析和图片识别。规则和结构化数据负责算，大模型负责解释，所以热量和营养素尽量来自数据库和业务逻辑，不完全交给模型自由发挥。”

操作：

- 进入 `/meals`
- 使用文本记录
- 点击解析并展示识别结果
- 确认保存

### Step 4. 进入 Agent Studio 发起复合任务

固定任务：

`我刚记录了晚餐，吃了180g鸡胸肉、100g米饭和一份生菜。请先分析今天还剩多少热量，再推荐一个适合减脂的加餐。`

讲法：

“这一步是固定 demo 的核心。它不是单轮问答，而是一个复合任务：先分析预算，再给推荐。系统会进入 planned mode，而不是 direct mode。”

操作：

- 打开 `/chat`
- 点击“固定 Demo 场景”快捷卡，或手动输入这段话

### Step 5. 展示 Agent 的计划与执行过程

讲法：

“当前项目的 Agent 不是靠包装，而是显式返回 `mode / plan / execution_trace`。也就是说，前端能直接展示它准备怎么做、实际做了什么。”

重点展示：

- `Planned Execution`
- `plan`
- `execution_trace`
- 如果触发澄清，也展示 `Clarification Needed`

### Step 6. 收束到周计划 / 周复盘

讲法：

“这个项目不止是记一次，而是把用户从建档、记录、建议一直带到阶段性复盘。周计划页就是把短期执行结果收束成长期反馈的地方。”

操作：

- 打开 `/review`
- 展示本周目标、日均缺口、每日状态、AI 总结

## 面试版 3 分钟讲稿

“这个项目我把它定位成一个移动端优先的 AI 健康教练 Agent Web 应用。它不是普通聊天 Demo，而是把健康管理的真实闭环做出来了：先建档，再记录餐食和运动，再通过 Agent 做预算分析、饮食建议和周复盘。

技术上最核心的是聊天链路。我把原来简单的意图分发，升级成了 `Router + Planner + Executor + Clarifier + Tool Registry`。对简单请求走 direct mode，对复合请求走 planned mode，前端还能展示 `plan` 和 `execution_trace`。如果信息不够，比如没有餐次、没有分量、没有训练上下文，系统会先澄清追问，而不是让模型硬猜。

为了让这个项目更像真实产品，我还把前端重做成了移动端优先的工作台和 Agent Studio，演示时可以完整走一条固定 demo 路线：记录一顿晚餐，分析剩余热量，再推荐一个适合减脂的加餐，最后回到周计划页做收束。”

## 演示时最该强调的 5 个点

- 不是裸聊，而是任务型 Agent
- 规则和结构化数据负责算，LLM 负责说
- 有 Clarification，不让模型在关键字段缺失时硬答
- 有显式记忆和执行 trace，便于解释和调试
- 前端是移动端产品形态，更像真实应用而不是后台系统

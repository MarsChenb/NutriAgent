# NutriAgent 简历表达

## 项目一句话

一个面向健康管理场景的移动端优先 AI Agent Web 应用，支持教练建档、多模态餐食记录、运动记录、热量缺口计算、任务型 Agent 对话与周计划 / 周复盘。

## 项目简介

基于 FastAPI + Next.js 开发的 AI 健康教练应用，面向减脂与健康管理场景，支持用户建档、文本/图片餐食记录、运动记录、热量缺口计算、个性化 AI 对话和周度复盘，形成从日常执行到阶段性反馈的健康管理闭环。

## 技术栈

Next.js, TypeScript, FastAPI, SQLAlchemy Async, PostgreSQL, pgvector, OpenAI-Compatible API, Prompt Engineering, RAG, Router + Planner + Executor

## 主要工作

1. 设计并实现健康管理闭环，围绕用户建档、餐食记录、运动记录、热量预算和周度复盘构建核心数据流，后端按日汇总摄入、消耗和热量缺口，用于首页工作台和 AI 建议生成。
2. 将原有 LLM 路由链路升级为任务型 Agent 架构，支持 `Direct / Planned / Clarification` 三种模式，并返回 `plan / execution_trace / context_snapshot` 供前端可视化展示。
3. 建立 Tool Registry，将餐食解析、热量查询、营养问答、知识检索、食谱推荐等能力统一封装为 Agent 可调用工具，支持复合任务拆解与逐步执行。
4. 引入 Human-in-the-Loop 澄清追问机制，在餐食记录、运动上下文不足和推荐任务缺参时主动追问，避免模型在关键信息缺失时硬答。
5. 实现显式记忆体系，短期记忆保存当前会话和澄清状态，长期记忆保存用户目标、口味偏好、过敏信息、饮食限制和健康史，用于后续推荐与建议生成。
6. 重做移动端优先前端界面，将首页、建档、记餐、运动记录、Agent Studio、周计划页面统一成更接近真实健康产品的视觉与交互形态。

## 面试亮点

- 我做的不是普通聊天页，而是一个任务型 Agent 工作台，用户高频需求都能收束到记录、分析、推荐和复盘这条链路上。
- 我把 Agent 执行过程显式返回给前端，面试时可以直接展示 `planned mode`、`plan` 和 `execution trace`，这比只展示模型回复更有说服力。
- 我区分了“规则负责算”和“模型负责说”，热量、缺口、营养结构等关键值尽量来自数据库与业务逻辑，不完全交给模型自由发挥。
- 我加了 Clarification 机制，在缺少餐次、分量或训练上下文时会先追问，这一点能明显提高可靠性，也很适合讲 Human-in-the-Loop。
- 我做了最小离线评估，至少能验证路由、计划模式、工具选择和澄清触发是否跑偏，不只是主观觉得“看起来能用”。

## 诚实可讲的不足

- 当前还是单用户作品集版本，没有扩展到多租户、权限和线上观测平台。
- 长期记忆目前是轻量实现，主要用于可解释上下文注入，还没有做更复杂的生命周期和检索策略。
- 评估是本地最小版本，适合回归，不等于线上真实流量下的完整效果评估。

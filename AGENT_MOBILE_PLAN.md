# NutriAgent Agent 升级与移动端重设计计划

## 1. 项目升级目标

把当前项目从一个“能运行的 AI 健康教练 Web 应用”，升级成一个：

- 对 `Agent 开发岗位` 有说服力的项目
- 对 `AI 应用开发岗位` 也足够完整的作品
- 在前端上具备明显 `移动端 App 感` 的演示效果

目标不是硬贴一个“多 Agent”标签，而是让项目真正具备多步任务拆解、工具调用、澄清追问、执行过程可视化和评估能力，同时把 UI 重做成更接近你给的那些截图风格。

最终想达到的产品形态：

`选择教练 -> 移动端建档 -> 今日工作台 -> 记录餐食/运动 -> Agent 拆解并执行复杂任务 -> 查看执行过程与建议 -> 周计划/周复盘`

---

## 2. 为什么这个项目完全可以包装成 Agent 项目

你这个项目其实比很多“挂着 Multi-Agent 名头”的项目更适合升级成 Agent 项目，原因很简单：

- 你已经有真实业务闭环，不是纯问答 Demo
- 你已经有多种工具能力的雏形
  - 餐食解析
  - 图片识别
  - 热量查询
  - 每日汇总
  - 运动记录
  - 知识检索
  - 食谱推荐
- 你的场景天然适合复杂任务
  - “记录饮食并分析剩余预算”
  - “结合今日运动推荐晚餐”
  - “如果我要减脂，今天接下来该怎么吃”
- 你的项目比纯知识问答更容易讲“工具编排”和“任务闭环”

所以重点不是“能不能包装”，而是：

`要把现在的固定分发流程，升级成真正可展示的 Agent workflow。`

---

## 3. 升级后的总路线

整个升级分成两条主线，同时推进：

### 主线 A：Agent 能力升级

- 从 `意图分类 + if/elif` 升级到 `Router + Planner + Executor + Summarizer`
- 建立统一 Tool Registry
- 增加 Human-in-the-Loop 澄清追问
- 增加记忆体系
- 增加 trace / 观测 / 评估

### 主线 B：移动端 Web UI 重做

- 以手机宽度为第一设计对象
- 学习你提供截图里的产品感
- 重做首页、建档、聊天页、复盘页
- 统一为轻盈、圆角、大留白、浅底色、强卡片感的移动端界面

---

## 4. 需要学习的 UI 风格特征

你给的截图有一套很明确的移动端健康产品设计语言，这次重做要主动学习这些特征，但不要照抄：

### 视觉方向

- 大面积浅色背景，不要深色大屏
- 模块化白色卡片，圆角很大
- 顶部信息轻，内容区重
- 数据展示以“大数字 + 小标签 + 轻进度条”为主
- 颜色以浅紫、浅蓝、薄荷绿、淡粉为主，不要杂乱
- 阴影轻，边框弱，整体通透感更强

### 交互方向

- 底部固定主操作区
- 首页像每日工作台，不像后台
- 建档流程是一题一屏或少量卡片选择
- Agent 聊天页不是技术面板堆砌，而是“像私教一样陪你做事”
- 技术细节可以折叠显示，不要默认把 trace 全糊在脸上

### 本项目的 UI 设计原则

- `移动端优先`
- `桌面端兼容`
- `AI 感要强，但不要像黑客控制台`
- `产品气质优先于技术炫耀`

---

## 5. 升级后的 Agent 架构目标

建议最终把后端收束成下面这套结构：

### 角色层

- `Router`
  - 判断简单任务还是复杂任务
- `Planner`
  - 对复杂任务拆解执行步骤
- `Executor`
  - 顺序或并行调用工具
- `Clarifier`
  - 用户信息不足时主动追问
- `Summarizer`
  - 生成最终用户可读结果
- `Memory Writer`
  - 把重要信息写入长期记忆

### 工具层

- `parse_meal_text`
- `recognize_food_from_image`
- `lookup_food_nutrition`
- `create_meal`
- `get_daily_summary`
- `list_recent_meals`
- `create_exercise`
- `list_recent_exercises`
- `retrieve_knowledge`
- `recommend_recipe`
- `get_weekly_review`
- `update_profile`

### 对前端可展示的结构

- 当前 intent
- 任务是否进入 planned execution
- 生成的 plan
- 调用了哪些工具
- 哪一步失败或触发澄清
- 最终结果

---

## 6. 分阶段开发计划

---

## Step 1. 建立真正的 Agent 主链路

### 目标

把当前 `/chat` 从“意图分类后直接调用固定 handler”，升级成：

- 简单任务：直接回答
- 复杂任务：Planner 拆解 + Executor 执行 + Summarizer 汇总

### 怎么做

- 保留现有简单任务直出能力，避免所有请求都走复杂链路
- 设计复杂任务识别规则
- 先支持 3 类复杂任务：
  - 记录饮食并分析今天剩余热量
  - 结合运动推荐一餐
  - 规划今天剩余饮食
- 返回结构增加：
  - `plan`
  - `execution_trace`
  - `mode`

### 交付结果

- 项目从 AI 应用升级为初步 Agent 项目
- 具备可讲的多步任务执行链路

### 实现提示词

```text
请把 NutriAgent 当前的 /chat 主流程升级成真正的 Agent 双模式架构，要求：
1. 保留简单请求的快速直出模式；
2. 对复杂请求增加 Planner + Executor + Summarizer 流程；
3. Planner 需要把任务拆成若干明确步骤，每一步标注 action/tool；
4. Executor 逐步执行计划，并记录结构化 execution trace；
5. /chat 返回 response、intent、mode、plan、execution_trace、context_snapshot；
6. 先优先支持“记录饮食并分析剩余预算”“结合运动推荐一餐”“规划今天剩余饮食”这几类复杂任务；
7. 尽量复用现有 meals / exercises / recipe / rag 逻辑，不要推倒重写。
```

---

## Step 2. 建立 Tool Registry

### 目标

把现有散落在服务层和 agent 层里的能力，统一成 Agent 可调用的工具。

### 怎么做

- 定义统一 tool schema
- 统一工具输入、输出和错误格式
- 把现有能力包装成 tool
- Executor 只通过 tool registry 执行

### 交付结果

- 多步任务执行的工具边界清晰
- 后续扩展多 Agent 更自然

### 实现提示词

```text
请为 NutriAgent 设计并实现统一的 Tool Registry，要求：
1. 定义工具注册结构，至少包含 name、description、input_schema、handler、retryable；
2. 将现有餐食解析、餐食保存、热量查询、每日汇总、最近餐食、最近运动、知识检索、食谱推荐等能力包装成 tools；
3. 统一工具输入校验和错误输出格式；
4. Executor 在执行 plan 时必须通过 tool registry 调用；
5. 保留现有业务层函数，通过适配层包装；
6. 完成后列出当前 tool 清单及其输入输出。
```

---

## Step 3. 增加澄清追问机制

### 目标

让 Agent 在信息不足时先追问，而不是硬答。

### 怎么做

- 补缺参检测
- 对餐食记录、运动记录、饮食推荐优先支持澄清
- 返回：
  - `requires_clarification`
  - `clarification_question`
  - `missing_fields`

### 交付结果

- 你能讲 HITL
- 系统回答会更稳

### 实现提示词

```text
请为 NutriAgent 的 Agent 流程加入 Human-in-the-Loop 澄清追问机制，要求：
1. 信息不足时不要直接执行工具；
2. 返回 requires_clarification、clarification_question、missing_fields 等结构化字段；
3. 优先支持餐食记录、运动记录、饮食推荐三类场景；
4. 澄清后用户继续输入时能衔接未完成任务；
5. 尽量复用现有对话上下文；
6. 完成后说明这套机制如何提升可靠性。
```

---

## Step 4. 增加短期记忆与长期记忆

### 目标

把“上下文拼接”升级为“可解释的记忆系统”。

### 怎么做

- 短期记忆
  - 当前未完成任务
  - 上轮 plan
  - 上轮 clarification
- 长期记忆
  - 用户目标
  - 偏好
  - 忌口
  - 常见饮食模式
  - 近期执行状态

### 交付结果

- 面试时可以讲 memory lifecycle
- 个性化更合理

### 实现提示词

```text
请为 NutriAgent 增加显式记忆体系，要求：
1. 区分短期记忆和长期记忆；
2. 短期记忆保存当前会话中的待完成任务、计划和澄清状态；
3. 长期记忆保存用户目标、偏好、忌口、常见行为模式等；
4. 设计 memory write / read 规则，不要全量盲注；
5. 推荐饮食、营养建议、周复盘时优先利用长期记忆；
6. 完成后说明什么该记、什么不该记。
```

---

## Step 5. 增加 trace 与可观测性

### 目标

让 Agent 的执行过程可见、可调试、可演示。

### 怎么做

- 每次 /chat 记录：
  - user input
  - intent
  - mode
  - plan
  - tool calls
  - step results
  - latency
  - error
- 提供 trace 查询接口
- 为前端展示做结构化输出

### 交付结果

- 项目具备 Agent 调试能力
- 前端可以把执行过程做成亮点

### 实现提示词

```text
请为 NutriAgent 增加 Agent 可观测性能力，要求：
1. 为每次 /chat 执行记录结构化 trace，包括用户输入、intent、mode、plan、tool calls、step results、latency、error；
2. 提供开发态 trace 查询接口；
3. 返回给前端的结构适合可视化展示；
4. 优先做轻量版本，不接第三方 tracing 平台；
5. 尽量复用现有接口和日志能力；
6. 完成后说明这部分如何帮助调试和面试展示。
```

---

## Step 6. 增加最小评估体系

### 目标

补齐“你怎么验证 Agent 效果”的面试点。

### 怎么做

- 构造固定 case
- 指标至少包括：
  - 意图分类正确率
  - 计划可执行率
  - 工具调用成功率
  - 复杂任务完成率
  - 澄清触发准确率

### 交付结果

- 项目工程可信度提升
- README 可写评估方法

### 实现提示词

```text
请为 NutriAgent 增加最小可用的 Agent 评估体系，要求：
1. 设计覆盖简单问答、复杂任务、缺参追问、工具失败的测试样例；
2. 至少评估意图分类正确率、计划可执行率、工具调用成功率、任务完成率、澄清触发准确率；
3. 先用本地脚本或 pytest 风格实现；
4. 输出结果要可读，便于写进 README；
5. 尽量复用现有接口，不重写业务；
6. 完成后说明每个指标的定义和解读方式。
```

---

## Step 7. 重做整体视觉系统，确定移动端产品语言

### 目标

先停止在单页上打补丁，统一定义全站视觉规范，解决“现在太丑”的根源问题。

### 怎么做

- 建立移动端优先的设计 token
  - 背景色
  - 主色
  - 强调色
  - 卡片圆角
  - 阴影层级
  - 字号层级
- 统一页面容器宽度
- 统一顶部区和底部固定操作区
- 统一按钮和卡片样式
- 解决乱码文案和视觉不一致

### 交付结果

- 后续页面重做不会风格乱飞
- 项目整体气质统一

### 实现提示词

```text
请先为 NutriAgent 建立一套移动端优先的视觉系统，要求：
1. 学习我给的健康类产品截图风格，但不要照抄；
2. 定义全站设计 token，包括背景、主色、强调色、圆角、阴影、字号、间距；
3. 统一页面容器、卡片、按钮、顶部区、底部固定操作区的样式语言；
4. 修复当前乱码文案和风格不统一问题；
5. 保持桌面端可用，但设计以手机宽度优先；
6. 完成后说明新的视觉方向和它与当前版本的差异。
```

---

## Step 8. 重做首页为移动端健康工作台

### 目标

把首页做成类似你给的截图那种“每日必开”的移动端工作台。

### 怎么做

- 顶部轻量导航
- 本周目标 + 日期横向切换
- 中部核心卡片展示：
  - 今日还可摄入热量
  - 饮食摄入
  - 运动消耗
  - 热量缺口
  - 三大营养素
+- 下方按餐次展示
- 底部固定主操作条：
  - 记餐
  - 相机
  - 进入 Agent

### 交付结果

- 首页会明显更像真实移动端产品
- 也是最重要的演示页之一

### 实现提示词

```text
请把 NutriAgent 首页重做成移动端优先的“今日健康工作台”，要求：
1. 参考我给的截图风格，做成浅底色、大圆角、强卡片感的移动端首页；
2. 顶部展示教练、周目标、日期切换；
3. 中部核心卡片展示剩余热量、饮食摄入、运动消耗、热量缺口和营养进度；
4. 下方按早餐/午餐/晚餐/加餐/运动分模块展示；
5. 底部固定主操作条，至少包含记餐、相机、进入 Agent；
6. 保持桌面端可用，但手机端优先；
7. 完成后说明首页的信息架构和交互设计。
```

---

## Step 9. 重做教练选择页与建档流程

### 目标

把 onboarding 做成真正有截图价值的移动端流程。

### 怎么做

- 教练选择页做成大卡片
- 建档页做成一题一屏或轻分步
- 底部固定下一步按钮
- 页面底部加入教练解释文案
- 选项卡片更像移动端产品，而不是普通表单

### 交付结果

- onboarding 成为非常适合录屏和截图的部分
- 项目完成度显著提升

### 实现提示词

```text
请重做 NutriAgent 的教练选择页和建档流程，要求：
1. 页面采用移动端优先布局；
2. 教练选择页使用大人物卡片，突出人格、风格和选中态；
3. 建档流程做成一题一屏或轻分步交互，底部固定“下一步”按钮；
4. 每一步页面底部都由当前教练给出一句解释或安抚文案；
5. 风格参考我给的截图，强调留白、圆角和轻盈感；
6. 不要做成后台表单；
7. 完成后说明这套 onboarding 为什么更适合产品展示。
```

---

## Step 10. 重做聊天页为移动端 Agent Studio

### 目标

这是整个项目最关键的一页，要同时解决两件事：

- 展示 Agent 能力
- 看起来像成熟的移动端 AI 私教产品

### 怎么做

- 页面上半部分展示对话
- 中间展示快捷任务
- 可折叠展示 plan / tools / execution trace
- 底部固定输入区
- 输入区像截图里那种移动端工具条，不像网页表单
- 清楚区分：
  - 直接回答
  - 多步执行
  - 需要澄清

### 交付结果

- 聊天页成为 Agent 项目的核心亮点页
- 面试时最适合展示

### 实现提示词

```text
请重做 NutriAgent 的聊天页，把它升级成移动端优先的 Agent Studio，要求：
1. 页面整体要像移动端 AI 私教产品，不是普通网页聊天框；
2. 展示对话区、快捷任务区、上下文摘要区，并支持折叠查看 plan / tool calls / execution trace；
3. 明确区分 Direct Answer、Planned Execution、Clarification Needed 三种状态；
4. 底部输入区固定，风格参考我给的截图，像 App 的主操作条；
5. 快捷任务卡片和追问卡片都要有产品感；
6. 修复当前乱码和半成品文案；
7. 完成后说明新的页面如何同时服务普通用户和面试演示。
```

---

## Step 11. 重做周计划 / 周复盘页

### 目标

让项目不仅能记，还能展示阶段性结果。

### 怎么做

- 参考你给的“7日计划”截图风格
- 强化周目标、日均缺口、达成率、状态标签
- 展示 AI 教练本周总结
- 增加“本周建议”和“继续问教练”入口

### 交付结果

- 形成完整闭环
- 页面也很适合截图

### 实现提示词

```text
请重做 NutriAgent 的周计划 / 周复盘页，要求：
1. 页面采用移动端优先布局，参考我给的 7 日计划类截图风格；
2. 展示本周目标、日均热量缺口目标、当前达成率、每日状态；
3. 强化 AI 教练总结区域，输出本周表现、问题和下周建议；
4. 提供“继续问教练”入口，把复盘和 Agent 聊天打通；
5. 页面视觉与首页、聊天页、建档页统一；
6. 完成后说明这一页如何帮助项目形成完整用户闭环。
```

---

## Step 12. 增加固定 Demo 场景与最终 README 打磨

### 目标

把项目变成真正可投递、可演示、可复盘的作品。

### 怎么做

- 设计一个固定复杂任务 Demo
- README 新增：
  - Agent 架构
  - Tool Registry
  - Trace 展示
  - 移动端产品截图位
  - 评估方法
- 输出简历版项目描述和面试亮点

### 交付结果

- 项目适合录屏和投递
- 讲述闭环完整

### 实现提示词

```text
请对 NutriAgent 做最后一轮 Agent 岗与移动端产品导向的打磨，要求：
1. 增加一个固定复杂任务 Demo，便于录屏和面试展示；
2. 更新 README，突出 Agent 架构、工具体系、trace、评估方法和移动端产品形态；
3. 预留或补充截图说明，让项目更像成熟作品；
4. 给我一版可直接写进简历的项目描述；
5. 给我 5 个面试时可重点展开的技术亮点；
6. 同时指出项目仍有哪些诚实可讲的不足。
```

---

## 7. 推荐执行顺序

建议按下面顺序推进：

1. Step 1 Agent 主链路
2. Step 2 Tool Registry
3. Step 3 澄清追问
4. Step 5 trace
5. Step 7 视觉系统
6. Step 8 首页
7. Step 9 建档
8. Step 10 聊天页
9. Step 4 记忆体系
10. Step 6 评估
11. Step 11 周计划 / 周复盘
12. Step 12 最终打磨

原因：

- 先把 Agent 骨架立住
- 再把移动端产品脸面做出来
- 然后补可观测性、记忆和评估，让它更像成熟项目

---

## 8. 最小可投递截止线

如果你想尽快把项目拉到“Agent 岗也能讲”的程度，最小版本建议先做到：

- Step 1
- Step 2
- Step 3
- Step 5
- Step 7
- Step 8
- Step 10
- Step 12 的 README / 简历版基础打磨

做到这里，你就已经不是“普通 AI 应用项目”了，而是：

`一个带多步任务执行、工具调用和过程展示的移动端 AI 健康教练 Agent 项目`

---

## 9. 面试里的最终项目定位

最终建议你把这个项目定位成：

`一个面向健康管理场景的移动端优先 AI Agent Web 应用。系统支持多模态餐食记录、运动记录、热量预算与周计划管理，并通过 Router + Planner + Executor 架构完成复杂任务拆解、工具调用与个性化建议生成。`

这个定位比单纯说“多 Agent”更稳，也更像一个真正做过产品和工程的人会说的话。
